(function(){
const c=s=>window.CURATED_STUDY.find(x=>x.slug===s),add=(s,i,h)=>c(s).lessons[i].body+=h;
add("aws",1,"<h2>S3 업로드의 실제 흐름</h2><ol><li>서버가 사용자와 파일 정책을 검증한다.</li><li>짧은 만료의 presigned URL을 발급한다.</li><li>브라우저가 S3로 직접 업로드한다.</li><li>완료 callback 또는 event에서 DB metadata를 확정한다.</li></ol><p>Object key는 서버가 생성하고 content type과 size를 제한한다. Lifecycle로 임시 파일과 오래된 version을 정리하며 CloudTrail·access log로 변경을 추적한다.</p><h2>Lambda 실패 복구</h2><p>재시도 가능한 예외와 영구 실패를 구분하고 DLQ에 event id와 원인을 남긴다. 같은 event가 두 번 실행되어도 결과가 중복되지 않도록 처리 이력을 기록한다.</p>");
add("infra",0,"<h2>서비스 등록 예시</h2><pre data-lang=\"INI\"><code>[Service]\\nUser=myapp\\nWorkingDirectory=/opt/myapp\\nEnvironmentFile=/etc/myapp/app.env\\nExecStart=/usr/bin/java -jar app.jar\\nRestart=on-failure</code></pre><p>설정 변경 후 daemon-reload와 restart를 수행하고 status·journal·curl health로 확인한다. 배포 전 artifact checksum과 실행 Java version을 기록하면 다른 파일 실행 문제를 줄인다.</p><h2>운영 파일 배치</h2><p>실행 파일은 /opt, 환경 설정은 /etc, service log는 journal 또는 /var/log처럼 역할별로 분리한다. 백업·복구 명령과 정기 보안 update 일정도 문서화한다.</p>");
add("realtime",0,"<h2>메시지 모델과 오류 응답</h2><p>메시지에는 messageId, roomId, senderId, type, payload, sentAt을 두고 server가 sender와 시간을 확정한다. client가 임의로 보낸 사용자 식별자를 신뢰하지 않는다. 검증 실패는 사용자 전용 error destination으로 돌려준다.</p><h2>확장 구조</h2><p>서버 한 대의 memory broker는 다른 instance의 구독자를 모른다. 외부 broker와 Redis/RabbitMQ relay를 사용하고 sticky session 여부, connection 수, outbound buffer와 느린 consumer 처리 정책을 측정한다.</p>");
const extras=[
["<h2>권장 버전 고정 파일</h2><pre data-lang=\"JSON\"><code>{ \"engines\": { \"node\": \">=20 <23\" }, \"scripts\": { \"dev\": \"vite\", \"build\": \"vite build\", \"lint\": \"eslint .\" } }</code></pre><p>새 clone에서는 <code>npm ci</code>로 lock file 그대로 설치한다. 개발 서버만 확인하지 말고 lint와 build까지 초기 기준으로 통과시킨다. 환경변수는 VITE_ 접두사만 browser bundle에 노출되므로 비밀을 넣지 않는다.</p>"],
["<h2>환경별 URL 구성</h2><pre data-lang=\"JavaScript\"><code>export const api = axios.create({ baseURL: import.meta.env.VITE_API_BASE_URL || '/api', timeout: 10000 });</code></pre><p>개발 proxy와 운영 reverse proxy의 경로를 같게 두면 code 변경이 줄어든다. preflight OPTIONS가 실패하면 허용 origin·method·header를 확인하고 wildcard origin과 credentials를 함께 쓰지 않는다.</p>"],
["<h2>초기 디렉터리 기준</h2><pre data-lang=\"Text\"><code>domain/      핵심 상태와 규칙\\nservice/     use case와 transaction\\nrepository/  persistence port\\ncontroller/  HTTP input/output\\nconfig/      framework 설정</code></pre><p><code>application-local.yml</code>은 local 기본값, 운영 비밀은 환경변수로 주입한다. Gradle wrapper를 commit해 설치된 Gradle version과 무관하게 build한다.</p>"],
["<h2>JSP 요청 확인 예시</h2><pre data-lang=\"Java\"><code>@GetMapping(\"/members\")\\nString members(Model model){\\n model.addAttribute(\"members\", service.findAll());\\n return \"member/list\";\\n}</code></pre><p>UTF-8 response, JSTL taglib, static resource 경로와 context path를 확인한다. JSP에 DB 호출이나 Java scriptlet을 넣지 않고 EL·JSTL로 출력한다.</p>"],
["<h2>스키마를 코드처럼 관리한다</h2><pre data-lang=\"SQL\"><code>CREATE TABLE member (\\n member_id NUMBER PRIMARY KEY,\\n email VARCHAR2(200) NOT NULL UNIQUE,\\n created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL\\n);\\nCREATE SEQUENCE member_seq;</code></pre><p>수동 변경만 남기지 않고 Flyway/Liquibase migration에 순번을 붙인다. rollback 또는 forward fix, test seed와 production data를 구분하고 backup 복원도 시험한다.</p>"],
["<h2>Mapper 연결 예시</h2><pre data-lang=\"XML\"><code>&lt;mapper namespace=\"com.example.MemberMapper\"&gt;\\n &lt;select id=\"findById\" resultMap=\"memberMap\"&gt;\\n  SELECT member_id, name FROM member WHERE member_id = #{id}\\n &lt;/select&gt;\\n&lt;/mapper&gt;</code></pre><p>interface signature와 namespace/id를 한 쌍으로 검사하고 SQL log에서 parameter와 query count를 확인한다. Service의 여러 mapper 변경은 같은 <code>@Transactional</code> 범위에 둔다.</p>"],
["<h2>첫 저장소 점검</h2><pre data-lang=\"Shell\"><code>git status\\ngit ls-files | grep -E '(\\.env|\\.pem|keystore)'\\ngit log --oneline --graph\\n./gradlew test  # 또는 npm test</code></pre><p>README에는 요구 version, 환경변수 이름, DB 초기화, 실행·test·build, port를 적는다. main direct push를 막고 PR build와 최소 한 명 review를 branch rule로 설정한다.</p>"],
["<h2>EC2 서비스 예시</h2><pre data-lang=\"Shell\"><code>sudo systemctl enable --now myapp\\nsudo systemctl status myapp\\nss -lntp | grep 8080\\ncurl -f http://127.0.0.1:8080/actuator/health</code></pre><p>배포 전에 이전 artifact를 보관하고 health 실패 시 자동 또는 수동 rollback 절차를 둔다. CloudWatch에서 CPU, memory agent, disk, 5xx, response time과 비용 budget을 감시한다.</p>"],
["<h2>Compose 준비 상태</h2><pre data-lang=\"YAML\"><code>healthcheck:\\n  test: [\"CMD-SHELL\", \"pg_isready -U app\"]\\n  interval: 5s\\n  timeout: 3s\\n  retries: 10</code></pre><p><code>docker compose config</code>로 최종 합성 설정을 보고, ps와 logs로 준비 상태를 확인한다. image build context에 .dockerignore를 두고 cache·secret·build output을 제외한다. Vagrant provisioning도 반복 실행해도 안전하게 작성한다.</p>"]
];
extras.forEach((x,i)=>add("setup",i,x[0]));
})();
