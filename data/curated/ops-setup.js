(function(){
const c=s=>window.CURATED_STUDY.find(x=>x.slug===s),set=(s,i,b)=>c(s).lessons[i].body=b;
const article=(intro,sections,summary)=>"<h2>먼저 이해할 핵심</h2><p>"+intro+"</p>"+sections.map(x=>"<h2>"+x[0]+"</h2>"+x[1]).join("")+"<blockquote><strong>복습 기준:</strong> "+summary+"</blockquote>";
set("git",0,article("Git은 파일을 복사해 보관하는 도구가 아니라 변경 상태를 작업 디렉터리·스테이징 영역·저장소의 세 단계로 관리하는 버전 관리 시스템이다.",[["세 영역과 기본 흐름","<div class=\"concept-flow\"><span>Working Tree</span><i>git add</i><span>Staging</span><i>git commit</i><span>Repository</span></div><pre data-lang=\"Shell\"><code>git status\\ngit diff\\ngit add src/\\ngit diff --staged\\ngit commit -m \"feat: 회원 검증 추가\"</code></pre>"],["좋은 커밋의 조건","<p>하나의 변경 이유만 담고 독립적으로 테스트·취소할 수 있어야 한다. 기능 변경과 포맷 변경을 섞지 않고 메시지에는 무엇을 왜 바꿨는지 남긴다.</p>"],["복구 명령의 차이","<p><code>restore</code>는 작업 파일, <code>restore --staged</code>는 staging을 되돌린다. 공유한 commit은 이력을 보존하는 <code>revert</code>로 취소하고, 이력을 재작성하는 reset은 공개 브랜치에서 피한다.</p>"],["비밀과 불필요한 파일 제외","<p>.gitignore에는 build 결과, node_modules, IDE 파일, .env와 키를 넣는다. 이미 commit된 비밀은 삭제만으로 과거 이력에서 사라지지 않으므로 즉시 폐기·교체한다.</p>"]],"add와 commit 전에 status와 diff로 실제 포함될 변경을 설명할 수 있어야 한다."));
set("git",1,article("브랜치는 프로젝트 복사본이 아니라 commit을 가리키는 가벼운 포인터이며, 기능별 변경을 격리하고 검토한 뒤 통합하기 위해 사용한다.",[["협업 흐름","<pre data-lang=\"Shell\"><code>git switch main\\ngit pull --ff-only\\ngit switch -c feature/member-search\\ngit push -u origin feature/member-search</code></pre><p>작은 commit과 Pull Request, CI, 리뷰를 거쳐 기본 브랜치에 병합한다.</p>"],["merge와 rebase","<table><tr><th>merge</th><td>분기와 통합 기록 보존</td></tr><tr><th>rebase</th><td>기준 위로 commit을 재작성해 선형 이력</td></tr></table><p>이미 공유한 commit을 rebase하면 다른 사람의 이력과 충돌하므로 개인 브랜치에서만 사용한다.</p>"],["충돌 해결","<p>양쪽 변경의 목적을 이해해 최종 코드를 만들고 marker 제거 후 테스트한다. 무조건 ours/theirs를 선택하면 조용한 기능 손상이 생긴다.</p>"],["fetch와 pull","<p>fetch는 원격 상태만 가져오고 pull은 이어서 merge 또는 rebase한다. 통합 전 원격 변경과 CI 결과를 확인한다.</p>"]],"브랜치 생성부터 충돌 해결·PR 병합까지 팀 규칙을 따라 재현할 수 있어야 한다."));
set("aws",0,article("EC2 배포는 서버 한 대를 켜는 작업이 아니라 사용자 요청이 VPC·subnet·route·보안 그룹을 지나 애플리케이션과 DB에 도달하는 경로를 설계하는 일이다.",[["네트워크 경로","<div class=\"concept-flow\"><span>DNS/HTTPS</span><i>→</i><span>Load Balancer</span><i>→</i><span>EC2</span><i>→</i><span>RDS</span></div><p>Security Group은 필요한 출발지와 포트만 허용한다. SSH와 DB를 전 세계에 공개하지 않는다.</p>"],["배포 순서","<ol><li>최소 권한 IAM Role과 네트워크 준비</li><li>runtime 또는 컨테이너 설치</li><li>artifact와 환경 설정 배치</li><li>systemd 자동 재시작 구성</li><li>TLS·health check·로그·알람 연결</li></ol>"],["비밀 관리","<p>키를 저장소나 이미지에 넣지 않고 IAM Role, Parameter Store, Secrets Manager에서 주입한다.</p>"],["장애 진단","<p>DNS → 보안 그룹 → 포트 리스닝 → 프로세스 로그 → DB 연결 순으로 범위를 좁힌다. 프로세스 실행만이 아니라 외부 health와 핵심 API로 성공을 판정한다.</p>"]],"요청 경로와 각 계층의 실패 확인 방법을 설명할 수 있어야 한다."));
set("aws",1,article("AWS 서비스는 이름이 아니라 데이터 형태, 실행 시간, 빈도, 일관성과 실패 복구 요구로 선택한다.",[["스토리지 비교","<table><tr><th>S3</th><td>업로드·정적 자원·백업용 Object</td></tr><tr><th>EBS</th><td>EC2에 연결하는 Block Disk</td></tr><tr><th>RDS</th><td>트랜잭션용 관리형 관계형 DB</td></tr></table><p>S3는 기본 비공개와 presigned URL, 암호화, 수명 주기를 사용한다.</p>"],["외부 API의 실패 설계","<p>연결·읽기 timeout, 제한된 재시도와 backoff, rate limit, 응답 검증을 둔다. POST 재시도는 중복 생성 방지 키가 필요하다.</p>"],["Lambda 사용 기준","<p>S3 이벤트 처리, 큐 소비, 짧은 일정 작업에 적합하다. cold start, 실행 제한, 동시성, 재시도 중복과 DLQ를 고려한다.</p>"],["관찰 가능성","<p>요청 ID, 실행 시간, 실패 횟수, 비용 지표와 알람을 남겨 자동 실행의 실패를 발견할 수 있게 한다.</p>"]],"서비스가 실패하거나 재실행될 때 데이터가 어떻게 되는지 답할 수 있어야 한다."));
set("infra",0,article("Linux 운영은 명령 암기가 아니라 프로세스·파일·네트워크·자원 상태를 출력으로 증명하며 장애 범위를 좁히는 능력이다.",[["기본 관찰","<pre data-lang=\"Shell\"><code>ps aux\\nss -lntp\\njournalctl -u myapp -f\\ndf -h\\nfree -h</code></pre>"],["권한과 실행 사용자","<p>root 대신 전용 사용자로 실행하고 소유자·그룹·rwx를 최소화한다. 설정과 비밀 파일은 읽을 주체를 제한한다.</p>"],["systemd 서비스","<p>실행 사용자, 작업 경로, 환경 파일, 재시작 정책을 선언한다. 프로세스·포트·health·로그를 함께 확인한다.</p>"],["장애 순서","<ol><li>CPU·메모리·디스크</li><li>프로세스와 재시작 횟수</li><li>포트와 방화벽</li><li>애플리케이션 로그</li><li>DB와 외부 연결</li></ol><p>로그 로테이션과 디스크 알람도 필수다.</p>"]],"현재 상태를 확인할 명령과 정상 출력의 의미를 설명할 수 있어야 한다."));
set("infra",1,article("Docker image는 실행 환경의 불변 템플릿이고 container는 그 image의 실행 인스턴스다. 코드뿐 아니라 runtime과 의존성을 함께 고정한다.",[["안전한 Image","<pre data-lang=\"Dockerfile\"><code>FROM eclipse-temurin:21-jre\\nWORKDIR /app\\nCOPY build/libs/app.jar app.jar\\nUSER 10001\\nENTRYPOINT [\"java\",\"-jar\",\"app.jar\"]</code></pre><p>작은 공식 image, 고정 버전, non-root를 사용하고 비밀은 image에 넣지 않는다.</p>"],["Volume과 Network","<p>영속 데이터는 volume에 두고 컨테이너끼리는 network의 서비스 이름으로 통신한다. 컨테이너 내부 localhost는 자기 자신이다.</p>"],["Compose 연결","<p>앱·DB·Redis의 port, environment, volume, healthcheck를 선언한다. depends_on은 DB 준비 완료를 보장하지 않아 healthcheck와 재시도가 필요하다.</p>"],["운영 기준","<p>로그는 stdout/stderr로 보내고 container 종료 신호를 처리한다. 새 PC에서 같은 명령으로 build와 health 확인이 되어야 한다.</p>"]],"image·container·volume·network의 수명과 관계를 설명할 수 있어야 한다."));
set("realtime",0,article("WebSocket은 한 연결에서 서버와 클라이언트가 양방향으로 메시지를 보내며, STOMP는 구독과 전송 destination 같은 메시지 규칙을 제공한다.",[["연결 흐름","<div class=\"concept-flow\"><span>Handshake</span><i>upgrade</i><span>CONNECT</span><i>→</i><span>SUBSCRIBE</span><i>↔</i><span>SEND</span></div>"],["인증과 권한","<p>연결뿐 아니라 메시지 전송과 방 구독마다 사용자 권한을 확인하고 입력 크기·형식·빈도를 제한한다.</p>"],["끊김과 재연결","<p>heartbeat로 죽은 연결을 감지하고 backoff와 jitter로 재연결 폭주를 막는다. 재연결 뒤 구독 복원과 중복·순서·누락 정책을 정한다.</p>"],["전달 보장","<p>중요 이벤트는 WebSocket 전송만 믿지 않고 DB 또는 broker에 저장한다. 여러 서버에서는 외부 broker로 메시지를 공유한다.</p>"]],"연결이 끊기거나 서버가 여러 대일 때 메시지의 운명을 설명할 수 있어야 한다."));
set("realtime",1,article("Redis는 String·Hash·Set·Sorted Set·Stream 같은 자료구조를 제공하는 메모리 저장소이며 캐시, 세션, 순위, 실시간 중계에 활용한다.",[["Cache Aside","<p>miss면 DB에서 읽어 저장하고, 변경 시 DB 반영 뒤 cache를 삭제한다. TTL에 분산값을 더하고 없는 값 캐시와 lock으로 동시 miss 폭주를 막는다.</p>"],["Pub/Sub과 Stream","<p>Pub/Sub은 오프라인 구독자가 메시지를 놓친다. 재처리와 소비 확인이 필요하면 Stream 또는 전문 broker를 사용한다.</p>"],["세션과 Token","<p>TTL을 실제 만료와 맞추고 key namespace, eviction 정책, 로그아웃 시 삭제를 설계한다.</p>"],["분산 Lock","<p>소유 token과 만료를 사용하고 원자적으로 해제한다. lock은 최후 수단이며 업무의 멱등성과 DB 제약을 함께 둔다.</p>"]],"Redis 데이터가 사라지거나 중복 전달되어도 시스템이 어떻게 회복하는지 답할 수 있어야 한다."));
const setupData=[
["Node·React/Vite","Node LTS 버전을 고정하고 node/npm 확인 후 Vite React template을 생성한다.","node -v와 npm -v → npm create vite → npm install → npm run dev → npm run build","HMR과 production build 성공","node_modules는 제외하고 lock file은 commit하며 Node 버전 차이를 막는다."],
["Vite Proxy","개발 중 React의 /api 요청을 Spring 포트로 전달한다.","vite.config proxy 설정 → 프론트는 상대 /api URL 사용 → backend path 확인 → 운영 reverse proxy 분리","Network 탭과 backend log에서 URL·상태·응답 확인","Vite proxy는 production build에서 동작하지 않으며 cookie 인증은 CORS·SameSite를 함께 맞춘다."],
["Spring Initializr","Java·Spring Boot·빌드 도구와 의존성의 기준 프로젝트를 만든다.","안정 Boot와 Java 21 선택 → Web/Validation/DB 의존성 선택 → 책임별 package → profile과 비밀 주입 설정","test, bootRun, build와 health endpoint 성공","Boot·Java·Gradle 호환성과 DataSource 자동 설정 실패를 확인한다."],
["Spring Boot JSP","JSP 기반 SSR View를 Spring Boot에 연결한다.","Jasper/JSTL 추가 → WEB-INF/views 배치 → prefix/suffix 설정 → @Controller에서 Model과 View 이름 반환","URL 요청 시 JSP 렌더링과 직접 View 접근 차단","@RestController 혼동과 jar/war별 JSP 경로를 확인한다."],
["Oracle Schema","관리 계정과 분리된 애플리케이션 사용자·스키마를 준비한다.","사용자 생성 → 최소 권한·quota → migration DDL → PK/FK/UNIQUE/sequence → seed","앱 계정으로 접속·DDL·CRUD·문자셋 확인","SYS를 앱에서 쓰지 않고 SID/service name과 listener를 구분한다."],
["MyBatis","DataSource부터 Mapper interface와 XML을 연결한다.","starter/driver → 환경별 DB 설정 → Mapper scan → namespace/id 일치 → resultMap → transaction","0건·단건·목록·변경 영향 행 수 통합 테스트","XML resource 경로와 #{ } binding을 확인한다."],
["Git 협업","새 저장소의 재현 가능한 협업 기준을 만든다.",".gitignore/README → init/add/diff/commit → origin → push → branch protection/CI","새 clone에서 README대로 test와 build 성공","env·pem·keystore를 commit하지 않고 현재 init 경로를 확인한다."],
["AWS 배포","EC2에서 DB·storage·외부 service가 연결된 앱을 안전하게 공개한다.","VPC/IAM/SG → runtime → artifact/config → systemd → RDS/S3 → DNS/HTTPS → monitor/backup","외부 health·핵심 API·DB 쓰기·재부팅 자동 시작","22·8080·DB를 전 세계에 열지 않고 비밀 저장소를 사용한다."],
["Docker·Vagrant","OS와 설치 차이를 제거해 팀 환경을 재현한다.","버전 고정 Dockerfile → Compose network/volume/health → env example → 필요 시 Vagrant provisioning → README","완전 정리 후 한 명령 build, health, data persistence","latest와 depends_on 준비 가정을 피하고 volume 삭제 전 데이터를 확인한다."]
];
setupData.forEach((d,i)=>set("setup",i,article(d[1],[["설정 순서","<ol>"+d[2].split(" → ").map(x=>"<li>"+x+"</li>").join("")+"</ol>"],["정상 동작 확인","<p>"+d[3]+"</p>"],["자주 막히는 지점","<p>"+d[4]+"</p>"],["프로젝트에 남길 기록","<p>버전, 실행 명령, 환경변수 이름, 포트, 정상 확인 URL, 초기화와 복구 절차를 README 또는 example 설정에 남긴다. 실제 비밀번호와 key는 기록하지 않는다.</p>"]],"새 환경에서 "+d[0]+" 설정을 재현하고 각 단계의 성공 여부를 확인할 수 있어야 한다.")));
})();
