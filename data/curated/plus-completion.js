/* 보충 자료도 레시피 이름만 남지 않도록 구현 전제와 실패 복구 기준을 채운다. */
(function () {
  const plus = (window.CURATED_STUDY || []).find((item) => item.slug === "plus-lab");
  const add = (index, html) => { if (plus?.lessons[index]) plus.lessons[index].body += html; };

  add(0, `<h2>PLUS LAB을 사용하는 순서</h2><ol><li>지금 해결할 문제가 배포·인증·결제·자동화·성능 중 어디에 속하는지 고른다.</li><li>레시피의 전체 흐름을 먼저 그리고 필요한 계정·키·callback URL을 준비한다.</li><li>가장 작은 정상 시나리오를 만든 뒤 실패·중복·재시도 시나리오를 추가한다.</li><li>secret은 서버에서만 관리하고 log와 응답에 노출되지 않는지 확인한다.</li><li>완성 뒤 설정 위치, 검증 명령, rollback 방법을 프로젝트 README로 옮긴다.</li></ol><p>레시피 코드를 그대로 붙여넣는 것이 목표가 아니다. 내 프로젝트의 사용자·주문·파일·배포 상태와 어디서 연결되는지 설명할 수 있을 때 적용이 끝난다.</p>`);
  add(1, `<h2>404를 막는 세 위치를 함께 맞춘다</h2><ul><li><strong>Vite base:</strong> project pages라면 저장소 이름을 포함한다.</li><li><strong>Router:</strong> BrowserRouter의 basename 또는 HashRouter 전략을 결정한다.</li><li><strong>GitHub Pages source:</strong> Actions가 만든 artifact를 Pages에 배포하도록 설정한다.</li></ul><p>workflow가 성공해도 CSS·image URL이 <code>/assets</code>로 시작하면 사용자 사이트 root를 바라볼 수 있다. 배포 주소에서 새로고침, 깊은 route 직접 접속, asset Network 200 응답까지 확인한다. custom domain을 쓰면 base와 DNS·HTTPS 상태를 다시 맞춘다.</p>`);
  add(2, `<h2>업로드 완료와 게시물 저장은 하나의 상태로 관리한다</h2><p>presigned URL 발급 성공만으로 게시물에 파일이 연결된 것은 아니다. client가 S3 upload를 완료한 뒤 object key와 size를 서버에 확정하고, 서버는 현재 사용자가 발급받은 key인지 검증한 뒤 DB에 기록한다. 게시물 저장이 취소되면 일정 시간이 지난 임시 object를 lifecycle 또는 정리 작업으로 삭제한다.</p>`);
  add(3, `<h2>OAuth callback에서 내 회원을 확정한다</h2><p>인가 코드는 browser가 받지만 token 교환은 client secret을 가진 backend가 수행한다. <code>state</code>를 검증해 로그인 요청 위조를 막고, provider user id를 기존 회원과 연결할 기준을 정한다. 이메일이 같다는 이유만으로 자동 병합하지 말고 이미 가입한 계정의 인증 절차를 거친다.</p><p>callback URL은 개발·운영 환경마다 provider console과 server 설정이 정확히 일치해야 한다. provider token을 내 Access Token처럼 client 저장소에 오래 보관하지 않는다.</p>`);
  add(4, `<h2>결제 API 응답보다 내 주문 상태가 기준이다</h2><div class="concept-flow"><span>주문 생성</span><i>READY</i><span>결제 준비</span><i>redirect</i><span>사용자 승인</span><i>APPROVE</i><span>PAID 확정</span></div><p>승인 API를 호출하기 전에 주문 금액·사용자·상품을 서버 DB에서 다시 읽고 provider가 돌려준 값과 비교한다. callback 중복, browser 새로고침, network timeout에도 한 주문이 두 번 승인되지 않도록 order id와 idempotency key를 사용한다.</p><p>승인은 성공했지만 DB 반영이 실패한 경우를 별도 상태로 남겨 재처리하거나 자동 취소할 수 있어야 한다. 실패·취소·환불도 단순 boolean이 아니라 상태 전이로 기록한다.</p>`);
  add(5, `<h2>외부 자동화는 중복과 호출 제한을 기본값으로 생각한다</h2><p>Notion 변경을 polling하거나 webhook으로 받더라도 event id와 마지막 처리 시각을 저장해 같은 이벤트를 두 번 실행하지 않는다. Slack 전송 실패는 즉시 무한 재시도하지 말고 rate limit의 Retry-After와 exponential backoff를 따른다. Notion page id, Slack channel id, 처리 결과와 오류 code는 남기되 token과 본문 속 개인정보는 log에서 제외한다.</p>`);
  add(6, `<h2>Observer callback과 실제 요청을 분리한다</h2><p>sentinel이 보이면 바로 여러 요청을 시작하지 말고 <code>isLoading</code>, <code>hasNext</code>, 현재 cursor를 확인한다. 응답이 오기 전에 observer가 반복 호출될 수 있으므로 진행 중 요청을 잠그고, component가 사라지면 observer와 AbortController를 정리한다.</p><p>offset page는 중간 데이터가 추가되면 중복·누락될 수 있다. 실시간으로 늘어나는 목록은 생성 시각과 고유 id를 함께 쓰는 cursor가 안정적이며, 마지막 id를 다음 요청에 전달한다.</p>`);
  add(7, `<h2>Filter의 성공과 실패 출구를 모두 그린다</h2><p>Authorization header가 없으면 공개 URL인지 판단해 다음 filter로 넘긴다. token이 있으면 형식·서명·만료를 검증하고, 유효할 때만 Authentication을 SecurityContext에 넣는다. 인증 실패 401과 권한 부족 403은 같은 handler에서 뭉개지 않고 client가 구분할 JSON code를 반환한다.</p><p>로그인 filter, JWT 검증 filter, exception handler의 순서를 test하고 Access Token 만료 뒤 Refresh 성공·실패·재사용 시나리오까지 확인한다.</p>`);
  add(8, `<h2>EC2에서 container를 교체하는 순서를 고정한다</h2><ol><li>CI가 test한 commit으로 image를 만들고 고정 tag와 digest를 push한다.</li><li>EC2가 새 image를 pull하되 기존 container는 아직 유지한다.</li><li>새 container를 다른 이름으로 실행하고 내부 health를 확인한다.</li><li>reverse proxy 또는 port를 새 container로 전환한다.</li><li>실패하면 이전 image로 즉시 복구하고 성공 뒤 오래된 container를 정리한다.</li></ol><p>환경 변수와 volume은 image 밖에 두고, registry login credential은 최소 권한과 만료 정책을 적용한다. 실행 중인 image digest를 배포 기록에 남겨야 실제 코드 버전을 추적할 수 있다.</p>`);
})();
